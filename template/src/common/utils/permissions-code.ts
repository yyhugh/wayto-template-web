export default
{
    // 系统配置（系统信息）。
    "config.manage": "config.manage.view",
    // 系统配置（设备设置）。
    "config.custom": "config.custom.view",
    // 系统配置（事件设置）。
    "config.user": "config.user.view",
    // 系统配置（人员设置）。
    "config.project": "config.project.view",
    // 系统配置（交接班设置）。
    "config.operation": "config.operation.view"
};
